---
phase: 02-deduplication-consolidation
plan: 05
type: execute
wave: 5
depends_on: [02-04]
files_modified:
  - photoconsole/cli.py
autonomous: true
requirements: [FR3, FR5]

must_haves:
  truths:
    - "'photoconsole report --config config.yaml' prints a duplicate group table with columns hash (12-char short), count, total_size, sources, date_taken range"
    - "'photoconsole report --config config.yaml --output-format csv' prints CSV rows to stdout with those same columns"
    - "'photoconsole report --config config.yaml --output-format json' prints valid JSON to stdout"
    - "'photoconsole plan-consolidation --config config.yaml' prints the consolidation plan (what would be copied) and exits 0 without writing any files"
    - "'photoconsole consolidate --config config.yaml --dry-run' prints the plan and 'Dry run complete' without writing files"
    - "'photoconsole consolidate --config config.yaml' (without --dry-run) prompts 'Proceed with consolidation?' before executing"
  artifacts:
    - path: "photoconsole/cli.py"
      provides: "report, plan_consolidation, consolidate click subcommands + _run_report, _run_consolidate helpers"
      contains: "def _run_report"
  key_links:
    - from: "photoconsole/cli.py:report command"
      to: "photoconsole/dedup.py:find_duplicate_groups"
      via: "_run_report calls find_duplicate_groups with session and source_priority from config"
      pattern: "find_duplicate_groups"
    - from: "photoconsole/cli.py:consolidate command"
      to: "photoconsole/consolidator.py:run_consolidation"
      via: "_run_consolidate calls run_consolidation with DuplicateGroup list and config"
      pattern: "run_consolidation"
---

<objective>
Extend photoconsole/cli.py with three new click subcommands: `report`, `plan-consolidation`,
and `consolidate`. All follow the established _run_scan / scan pure-helper + click-wrapper
pattern from Phase 1. No click API in the _run_* helpers — only pure logic.

Purpose: Expose the dedup and consolidation pipeline through the CLI (FR5).
Output: photoconsole/cli.py extended with _run_report, _run_consolidate, and three commands.
</objective>

<execution_context>
@C:/Users/jeffr/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/jeffr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-deduplication-consolidation/02-CONTEXT.md
@.planning/phases/02-deduplication-consolidation/02-RESEARCH.md
@.planning/phases/02-deduplication-consolidation/02-04-SUMMARY.md

<interfaces>
From photoconsole/cli.py (existing — extend, do not replace):
```python
_BATCH_SIZE = 50

@click.group()
@click.option("-v", "--verbose", is_flag=True)
@click.option("-q", "--quiet", is_flag=True)
@click.pass_context
def main(ctx, verbose, quiet): ...

@main.command()
@click.option("--config", "config_path", required=True,
              type=click.Path(exists=True, dir_okay=False, readable=True))
@click.pass_context
def scan(ctx, config_path): ...

def _run_scan(config_path, verbose=False, quiet=False) -> dict: ...
def _print_summary(summary): ...
```

From photoconsole/dedup.py (Plan 02 output):
```python
def find_duplicate_groups(session: Session, source_priority: list[str]) -> list[DuplicateGroup]: ...

@dataclass
class DuplicateGroup:
    hash: str; canonical: MediaFile; redundants: list[MediaFile]; needs_copy: bool
```

From photoconsole/consolidator.py (Plan 04 output):
```python
def run_consolidation(groups, config, dry_run=False, log_path=None) -> ConsolidationPlan: ...

@dataclass
class ConsolidationPlan:
    to_copy: list[CopyAction]
    to_manifest: list[dict]
    skipped: list[CopyAction]
    errors: list[CopyAction]
```

From photoconsole/catalog/db.py:
```python
def create_catalog_engine(db_path: str | Path) -> Engine: ...
def session_factory(engine: Engine) -> sessionmaker: ...
```

Report output format (D-14 + RESEARCH.md Pattern 10):
- text mode: rich.table.Table with columns Hash(12-char), Count, Total Size, Sources, Date Range
- csv mode: csv.writer to stdout, header + one row per group
- json mode: json.dumps list of dicts with keys hash, count, total_size_bytes, sources, date_range

CLI patterns (from RESEARCH.md Pattern 10):
- All three commands use @click.pass_context and read ctx.obj['verbose'], ctx.obj['quiet']
- --config option on every command: type=click.Path(exists=True, dir_okay=False, readable=True)
- consolidate command has --dry-run flag; uses click.confirm('Proceed with consolidation?', abort=True) before live run
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: _run_report() and report command</name>
  <files>photoconsole/cli.py</files>
  <read_first>
    photoconsole/cli.py
    photoconsole/dedup.py
    photoconsole/config.py
    photoconsole/catalog/db.py
  </read_first>
  <behavior>
    - _run_report(config_path, output_format='text', verbose=False, quiet=False) returns list[DuplicateGroup]
    - _run_report opens a catalog session, calls find_duplicate_groups(session, config.consolidation.source_priority)
    - report command with --output-format text prints a rich.table.Table with title 'Duplicate Groups' and columns Hash, Count, Total Size, Sources, Date Range (D-14)
    - report command with --output-format csv prints a CSV header 'hash,count,total_size,sources,date_range' followed by data rows to stdout
    - report command with --output-format json prints valid JSON array where each item has keys hash, count, total_size_bytes, sources, date_range
    - Hash column shows first 12 characters of the SHA-256 hash string
    - Total size is displayed in MB for text mode; raw bytes for json mode
    - Date Range shows 'min_date .. max_date' for groups with date_taken values; 'unknown' when all are None
    - report command with no duplicates in catalog prints table/JSON with zero rows and exits 0
  </behavior>
  <action>
    Add _run_report(config_path: str, output_format: str = 'text', verbose: bool = False,
    quiet: bool = False) -> list[DuplicateGroup] to photoconsole/cli.py.
    Implementation: load_config(config_path) → create_catalog_engine → session_factory →
    with SessionLocal() as session: groups = find_duplicate_groups(session, cfg.consolidation.source_priority).
    Return groups.

    Add _print_report(groups: list[DuplicateGroup], output_format: str, quiet: bool) helper.
    For 'text' mode: build rich.table.Table(title='Duplicate Groups'), add columns Hash (style='cyan', no_wrap=True),
    Count (justify='right'), Total Size (justify='right'), Sources, Date Range.
    For each group: all_files = [group.canonical] + group.redundants;
    total_size = sum(f.size or 0 for f in all_files);
    sources = ', '.join(sorted({f.source_name for f in all_files if f.source_name}));
    dates = [f.date_taken for f in all_files if f.date_taken];
    date_range = f'{min(dates)} .. {max(dates)}' if dates else 'unknown';
    table.add_row((group.hash or '')[:12], str(len(all_files)), f'{total_size/1_048_576:.1f} MB', sources, date_range).
    Console().print(table).
    For 'csv' mode: import csv, sys; writer = csv.writer(sys.stdout);
    writer.writerow(['hash', 'count', 'total_size', 'sources', 'date_range']);
    for each group: writer.writerow([(group.hash or '')[:12], len(all_files), total_size, sources, date_range]).
    For 'json' mode: import json; rows = [{'hash': ..., 'count': ..., 'total_size_bytes': total_size,
    'sources': sources, 'date_range': date_range} for group in groups]; print(json.dumps(rows, indent=2)).

    Add report command decorated with @main.command(), @click.option('--config', ...),
    @click.option('--output-format', default='text', type=click.Choice(['text', 'csv', 'json'])),
    @click.pass_context. Body: groups = _run_report(config_path, output_format, verbose, quiet);
    if not quiet: _print_report(groups, output_format, quiet).

    Import find_duplicate_groups from photoconsole.dedup at the top of cli.py.
    Import rich.table.Table and rich.console.Console inside _print_report for 'text' mode
    (to avoid import-time hard dependency).
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_phase2.py::test_report_text tests/test_cli_phase2.py::test_report_json tests/test_cli_phase2.py::test_report_csv -x -q 2>/dev/null || python -c "from photoconsole.cli import _run_report; print('_run_report importable')"</automated>
  </verify>
  <acceptance_criteria>
    - _run_report is defined in photoconsole/cli.py and returns list[DuplicateGroup]
    - report command is registered on the main click group
    - report command accepts --output-format with choices text, csv, json
    - text output uses rich.table.Table with columns Hash, Count, Total Size, Sources, Date Range
    - csv output writes header hash,count,total_size,sources,date_range to stdout
    - json output is a JSON array parseable by json.loads()
    - python -c "from photoconsole.cli import main" succeeds without ImportError
  </acceptance_criteria>
  <done>report command implemented with text/csv/json output modes (D-14); _run_report is a pure helper decoupled from click.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: _run_consolidate(), plan-consolidation command, consolidate command</name>
  <files>photoconsole/cli.py</files>
  <read_first>
    photoconsole/cli.py
    photoconsole/consolidator.py
    photoconsole/dedup.py
  </read_first>
  <behavior>
    - _run_consolidate(config_path, dry_run=True, verbose=False) calls find_duplicate_groups then run_consolidation(groups, config, dry_run=dry_run) and returns ConsolidationPlan
    - plan-consolidation command always passes dry_run=True to _run_consolidate; prints plan summary; exits 0 without writing files
    - consolidate --dry-run passes dry_run=True; prints plan and 'Dry run complete — no files written'; exits 0
    - consolidate (without --dry-run) prints the plan, then prompts 'Proceed with consolidation?' via click.confirm(abort=True), then calls _run_consolidate with dry_run=False
    - The plan summary shows: N files to copy, N already present (skipped), N manifest entries, N errors
    - If config.consolidation.destination_path is empty, _run_consolidate raises RuntimeError with message containing 'destination_path'
  </behavior>
  <action>
    Add _run_consolidate(config_path: str, dry_run: bool = True, verbose: bool = False) -> ConsolidationPlan.
    Implementation: cfg = load_config(config_path); engine = create_catalog_engine(cfg.catalog_path);
    SessionLocal = session_factory(engine);
    with SessionLocal() as session: groups = find_duplicate_groups(session, cfg.consolidation.source_priority).
    plan = run_consolidation(groups, cfg, dry_run=dry_run).
    Return plan.

    Add _print_plan(plan: ConsolidationPlan) helper.
    Prints: 'Consolidation plan:' header, then:
    '  Files to copy  : N' (len(plan.to_copy)),
    '  Already present: N' (len(plan.skipped)),
    '  Manifest entries: N' (len(plan.to_manifest)),
    '  Errors         : N' (len(plan.errors)).
    If plan.to_copy is non-empty and verbose: print each CopyAction src_path → dest_path.

    Add plan_consolidation command: @main.command(name='plan-consolidation'),
    @click.option('--config', ...), @click.pass_context.
    Body: plan = _run_consolidate(config_path, dry_run=True, verbose=ctx.obj['verbose']);
    _print_plan(plan); click.echo('Dry run complete — no files written.').

    Add consolidate command: @main.command(), @click.option('--config', ...),
    @click.option('--dry-run', is_flag=True, default=False), @click.pass_context.
    Body: plan = _run_consolidate(config_path, dry_run=True, verbose=ctx.obj['verbose']);
    _print_plan(plan).
    If dry_run: click.echo('Dry run complete — no files written.'); return.
    click.confirm('Proceed with consolidation?', abort=True).
    plan = _run_consolidate(config_path, dry_run=False, verbose=ctx.obj['verbose']);
    _print_plan(plan); click.echo(f'Consolidation complete. {len(plan.to_copy)} file(s) copied, {len(plan.errors)} error(s).').

    Import run_consolidation and ConsolidationPlan from photoconsole.consolidator at top of cli.py.
  </action>
  <verify>
    <automated>python -m pytest tests/test_cli_phase2.py::test_plan_consolidation tests/test_cli_phase2.py::test_consolidate_dry_run -x -q 2>/dev/null || python -c "from photoconsole.cli import _run_consolidate, main; print('_run_consolidate importable')"</automated>
  </verify>
  <acceptance_criteria>
    - _run_consolidate is defined in photoconsole/cli.py
    - plan-consolidation command is registered on main click group (name='plan-consolidation')
    - consolidate command is registered on main click group with --dry-run flag
    - consolidate command uses click.confirm('Proceed with consolidation?', abort=True) before live run
    - plan-consolidation and consolidate --dry-run write no files to disk
    - 'photoconsole --help' lists report, plan-consolidation, consolidate in command list
    - python -c "from photoconsole.cli import _run_consolidate" succeeds
  </acceptance_criteria>
  <done>plan-consolidation (always dry_run) and consolidate (with --dry-run flag + confirmation gate) implemented; _run_consolidate is a pure helper (D-16, FR5).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI → _run_consolidate | User-invoked; consolidate command requires explicit confirmation before live run |
| click.confirm → user input | Standard click confirmation; abort=True handles Ctrl+C gracefully |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | consolidate: live run without confirmation | mitigate | click.confirm(..., abort=True) always shown before _run_consolidate(dry_run=False); plan-consolidation is always dry_run=True |
| T-05-02 | Information Disclosure | report: full file paths in rich table output | accept | Local-only CLI; output is the intended user-facing view of their own file paths |
| T-05-03 | Tampering | config_path validated by click.Path(exists=True) | mitigate | Same validation used in scan command (T-05-01 in Phase 1); already established pattern |
| T-05-SC | Tampering | npm/pip/cargo installs | accept | No new installs; rich and click already installed |
</threat_model>

<verification>
python -c "from photoconsole.cli import main, _run_report, _run_consolidate; print('OK')"
python -m pytest tests/test_cli.py -x -q   # existing scan tests must still pass
</verification>

<success_criteria>
- report, plan-consolidation, consolidate commands registered on main click group
- _run_report and _run_consolidate are pure helpers (no click API calls inside)
- report supports --output-format text/csv/json (D-14)
- plan-consolidation always dry_run=True (D-16)
- consolidate uses click.confirm before live run (FR4 safety requirement)
- All existing tests/test_cli.py tests still pass (scan command unchanged)
</success_criteria>

<output>
Create .planning/phases/02-deduplication-consolidation/02-05-SUMMARY.md when done.
</output>
