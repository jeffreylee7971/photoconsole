---
phase: 03-visual-clustering-ai-integration
plan: 06
type: execute
wave: 4
depends_on:
  - 03-04
  - 03-05
files_modified:
  - photoconsole/cli.py
  - tests/test_cli_phase3.py
autonomous: true
requirements:
  - FR5
  - NFR1
  - NFR2
  - FR6
user_setup: []
tags:
  - cli
  - click
  - rich
  - integration
must_haves:
  truths:
    - "photoconsole cluster runs the full two-pass pipeline (quality -> pHash -> CLIP embedding -> DBSCAN) and writes cluster_id to the catalog"
    - "The cluster command auto-switches to cluster_embeddings_sparse when len(files_with_embed) > ClusterConfig.sparse_threshold"
    - "The cluster command honors --threshold flag overriding ClusterConfig.eps"
    - "The cluster command skips files where embedding_blob IS NOT NULL (D-03 incremental)"
    - "The cluster command invalidates embedding_blob + phash when on-disk mtime differs from catalog mtime (RESEARCH.md Pattern 3)"
    - "photoconsole review-clusters queries clusters WHERE cluster_id IS NOT NULL AND cluster_keeper IS NULL"
    - "review-clusters opens the top 2-3 candidates per cluster via os.startfile wrapped in try/except OSError"
    - "review-clusters writes cluster_keeper=True for the chosen file and cluster_redundant=True for the rest"
    - "review-clusters generates a deletion manifest via write_cluster_manifest after the session ends"
    - "Both commands use rich.progress.track (NOT tqdm) for progress and rich.table.Table for tabular output"
    - "All DB writes happen on the main thread (T-02-02); ThreadPoolExecutor is used only for quality scoring workers"
    - "Preflight gates raise clear errors when opencv-python-headless or open-clip-torch is missing"
  artifacts:
    - path: "photoconsole/cli.py"
      provides: "_run_cluster + cluster click command + _run_review + review-clusters click command + _print_cluster_summary"
      exports: ["_run_cluster", "_run_review"]
    - path: "tests/test_cli_phase3.py"
      provides: "CliRunner integration tests for cluster and review-clusters; preflight gates; incremental skip; keeper persistence; manifest writing"
      contains: "test_cluster_skips_cached"
  key_links:
    - from: "photoconsole/cli.py:_run_cluster"
      to: "photoconsole.quality.compute_quality_scores + photoconsole.clustering.embed_images + cluster_embeddings"
      via: "function calls"
      pattern: "compute_quality_scores\\("
    - from: "photoconsole/cli.py:_run_review"
      to: "os.startfile + write_cluster_manifest"
      via: "Windows shell + manifest writer"
      pattern: "os\\.startfile"
    - from: "photoconsole/cli.py:cluster command"
      to: "rich.progress.track + rich.table.Table"
      via: "Rich console output"
      pattern: "rich\\.progress\\.track"
---

<objective>
Extend `photoconsole/cli.py` with two new subcommands — `cluster` and `review-clusters` — each built as a thin click wrapper over a pure helper (`_run_cluster` / `_run_review`) following the established `_run_scan` / `_run_report` pattern from Phase 1/2.

Purpose: This plan wires together every other Phase 3 plan into the end-user experience. The `cluster` command runs the full quality + pHash + embedding + DBSCAN pipeline (D-01/D-02/D-04); the `review-clusters` command surfaces clusters via Rich tables, opens Windows Photos for visual comparison (D-13), captures the user's keeper choice (D-14/D-15), and writes the final deletion manifest (D-16). After this plan ships, the phase goal is functionally achievable from the command line.

Output: Extended `photoconsole/cli.py` with `_run_cluster`, `_run_review`, `_print_cluster_summary` helpers plus two new click commands; new `tests/test_cli_phase3.py` with CliRunner integration tests covering preflight, incremental skip, keeper persistence, manifest writing, and sparse fallback dispatch.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-clustering-ai-integration/03-CONTEXT.md
@.planning/phases/03-visual-clustering-ai-integration/03-RESEARCH.md
@.planning/phases/03-visual-clustering-ai-integration/03-PATTERNS.md
@.planning/phases/03-visual-clustering-ai-integration/03-AI-SPEC.md

@photoconsole/cli.py
@photoconsole/catalog/db.py
@photoconsole/catalog/models.py
@photoconsole/config.py
@photoconsole/quality.py
@photoconsole/clustering.py
@tests/test_cli_phase2.py
@tests/test_cli.py

<interfaces>
Existing cli.py structure (read in full earlier):
- Click group `main` (lines 69-83) with --verbose / --quiet flags stored in ctx.obj
- `_BATCH_SIZE = 50` at line 61
- `_run_scan(config_path, verbose, quiet)` pure helper at line ~143 — pattern source: load_config -> ffprobe gate -> create_catalog_engine -> session_factory -> scan_all -> ThreadPoolExecutor batched UPSERT
- `_flush_buffer` nested function inside _run_scan (lines 230-243) — single-writer flush pattern
- `_run_report(config_path, verbose, quiet) -> list[DuplicateGroup]` pure helper at line ~325
- click commands: scan, report, plan-consolidation, consolidate
- ffprobe preflight gate at lines 173-184 — pattern source for the new cv2 + open_clip import gates

New imports needed:
- from photoconsole.quality import compute_quality_scores, normalize_scores, compute_combined_score, exif_completeness_score
- from photoconsole.clustering import (ClusterGroup, load_clip_model, embed_images, compute_phash, cluster_embeddings, cluster_embeddings_sparse, embedding_to_blob, blob_to_embedding, write_cluster_manifest)
- from photoconsole.catalog.models import MediaFile
- from photoconsole.catalog.db import upsert_media_file
- from sqlalchemy import select
- import os (already imported in cli.py? confirm during edit)
- Lazy inside helpers: from rich.console import Console; from rich.table import Table; from rich.progress import track

CliRunner test pattern source: tests/test_cli_phase2.py (read in full).
- `_write_config(config_path, catalog_path, source_path, **kwargs)` helper — writes minimal YAML
- `_seed_*` helpers that pre-populate the catalog before invocation
- `CliRunner().invoke(main, args, input="...")` for interactive prompt simulation

Review prompt protocol (per D-14 + AI-SPEC):
- For each ClusterGroup: print Rich table with rows for each member (cluster_id, path truncated, sharpness_norm, resolution_norm, faces_norm, exif_score, combined_score; mark auto-pick row with [AUTO-PICK])
- os.startfile on the top 2-3 candidates (sorted by combined_score desc, slice [:3])
- click.prompt with default="1" -> "1" or empty = auto-pick; "2"/"3"/N = override; "s" = skip
- Persist via upsert_media_file inside SessionLocal context: chosen.cluster_keeper=True; for each non-keeper: cluster_redundant=True

End-of-run summary (per AI-SPEC §7):
- Files total, files quality-scored, files skipped cached, files embedded, files skipped embedded, clusters found, noise count, largest cluster size, oversized clusters (>50), timing breakdown (quality / embed / dbscan / total), config used (eps, phash_threshold, weights)

Per RESEARCH.md Pattern 3 (embedding invalidation): before the embed pass, query files where embedding_blob IS NOT NULL; for each, compare Path(mf.path).stat().st_mtime to mf.mtime; if differs by > 0.01: nullify embedding_blob and phash via upsert_media_file. Then proceed to embed pass.

Per RESEARCH.md Q4/Pattern 5: rclone path detection for BAT routing — pass source_type field through write_cluster_manifest rows (already supported via extrasaction="ignore").

Per AI-SPEC Section 6 guardrails:
- Preflight: try cv2 import; try open_clip lazy load; verify catalog file exists
- Per-cluster gate: cluster size > 50 -> log warning, flag in summary; do not crash
- Zero-vector embedding skip: skip files whose embedding L2 norm < 0.01 from clustering input
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement _run_cluster pure helper + cluster click command + _print_cluster_summary in cli.py</name>
  <files>photoconsole/cli.py</files>
  <read_first>
    - photoconsole/cli.py (read in full — extension points: imports at top, helpers near _run_scan, new commands after `consolidate`)
    - photoconsole/quality.py (Plan 04 — public API)
    - photoconsole/clustering.py (Plan 05 — public API)
    - photoconsole/catalog/db.py (after Plan 02 — _ensure_phase3_columns called from create_catalog_engine)
    - photoconsole/config.py (after Plan 03 — ClusterConfig + QualityWeightsConfig fields)
    - .planning/phases/03-visual-clustering-ai-integration/03-PATTERNS.md (`photoconsole/cli.py` section)
    - .planning/phases/03-visual-clustering-ai-integration/03-AI-SPEC.md (Section 4 _run_cluster shape; Section 7 end-of-run summary)
    - .planning/phases/03-visual-clustering-ai-integration/03-RESEARCH.md (Pattern 3 embedding invalidation; Q6 no schema versioning; Q10 batch writes)
  </read_first>
  <behavior>
    - `photoconsole cluster --help` exits 0 and lists --config and --threshold options
    - `photoconsole cluster --config <yaml>` on a fresh catalog runs to completion and writes cluster_id values
    - On second invocation with no file changes, files with embedding_blob NOT NULL are skipped (zero new embeddings reflected in summary)
    - On invocation where a file's mtime changed, embedding_blob + phash are nullified before the embed pass
    - --threshold 0.30 overrides ClusterConfig.eps for the DBSCAN call
    - Preflight: missing cv2 -> RuntimeError with install hint; missing open_clip -> RuntimeError with install hint (raised inside load_clip_model and re-raised by _run_cluster with context)
    - Catalogs with files_with_embed > ClusterConfig.sparse_threshold dispatch to cluster_embeddings_sparse (verified by log message + ability to mock and assert which fn was called)
    - All DB commits happen on the main thread (T-02-02)
    - End-of-run Rich summary table printed unless --quiet
    - Return value is a dict with keys: files_quality_computed, files_embedded, files_skipped_cached, clusters_found, noise_count, max_cluster_size, oversized_clusters, elapsed_seconds, used_sparse (bool)
  </behavior>
  <action>
    Extend the existing cli.py — append imports, helpers, and click commands. DO NOT replace existing scan/report/plan-consolidation/consolidate commands.

    Add new imports near line 58 (preserving existing import grouping):
      - import os (verify if already imported; add if not)
      - from sqlalchemy import select
      - from photoconsole.catalog.models import MediaFile
      - from photoconsole.quality import compute_quality_scores, normalize_scores, compute_combined_score, exif_completeness_score
      - from photoconsole.clustering import (ClusterGroup, load_clip_model, embed_images, compute_phash, cluster_embeddings, cluster_embeddings_sparse, embedding_to_blob, blob_to_embedding, write_cluster_manifest)
      - from photoconsole.catalog.db import upsert_media_file
      - Inside _run_cluster body (lazy): from rich.console import Console; from rich.table import Table; from rich.progress import track

    Add module-level constant near _BATCH_SIZE: _PHASE3_BATCH_SIZE = 50.

    Define `_run_cluster(config_path: str, threshold: float | None = None, verbose: bool = False, quiet: bool = False) -> dict`:

      Step 1 — preflight + thread guard:
        - assert threading.current_thread() is threading.main_thread() with message "cluster must run on main thread"
        - Try import cv2; on ImportError raise RuntimeError("opencv-python-headless is required for 'cluster'. Install: pip install opencv-python-headless")
        - cfg = load_config(config_path)
        - If threshold is not None: log info and bind local eps = threshold else eps = cfg.cluster.eps

      Step 2 — engine + session:
        - engine = create_catalog_engine(cfg.catalog_path)  -- triggers _ensure_phase3_columns
        - SessionLocal = session_factory(engine)
        - start_time = time.monotonic()

      Step 3 — Pass 0 quality scoring (incremental):
        - Open session, query MediaFile rows where blur_score IS NULL AND status == 'ok'; collect tuples (id, path, date_taken, gps_lat, gps_lon); close session.
        - If list non-empty:
          - paths = [p for (_id, p, *_) in records]
          - Run compute_quality_scores(paths, max_workers=cfg.cluster.max_workers, progress_cb=...) with rich.progress.track wrapping the iterator (description "Computing quality scores").
          - Buffer results; flush in batches of _PHASE3_BATCH_SIZE via upsert_media_file({"path": r["path"], "blur_score": r["blur_score"], "pixel_width": r["pixel_width"], "pixel_height": r["pixel_height"], "face_count": r["face_count"]}) only for rows where error is None. Error rows: log warning, leave columns NULL.
          - Commit at each flush; final flush after loop.
          - files_quality_computed = len(records); quality_elapsed measured.

      Step 4 — Pass 1 pHash incremental:
        - Query MediaFile rows where phash IS NULL AND status == 'ok'; for each call clustering.compute_phash(path); buffer; flush in batches.
        - Use ThreadPoolExecutor(max_workers=cfg.cluster.max_workers) for parallelism with main-thread upsert. Document choice in code comment.
        - All futures are submitted from the main thread; results collected via as_completed() on the main thread; upsert_media_file() and session.commit() called on the main thread only — never inside the executor (T-02-02 pattern from RESEARCH.md).

      Step 5 — Pass 2 embedding invalidation + CLIP embeddings:
        - Query MediaFile rows with embedding_blob.isnot(None); for each compare Path(mf.path).stat().st_mtime vs mf.mtime; if differs > 0.01: collect. Wrap stat() in try/except OSError (file gone: leave alone).
        - For each invalidated path: upsert_media_file({"path": p, "embedding_blob": None, "phash": None}); commit at end of invalidation pass. Log invalidation count.
        - Query MediaFile rows where embedding_blob IS NULL AND status == 'ok' AND path IS NOT NULL; collect (id, path) tuples.
        - If non-empty:
          - model, preprocess = load_clip_model() — wrap in try/except ImportError, re-raise as RuntimeError with install hint
          - paths_to_embed = [p for (_id, p) in records]
          - embeddings = embed_images(paths_to_embed, model, preprocess, device="cpu", batch_size=cfg.cluster.batch_size)
          - For each (id, path), emb pair: buffer {"path": path, "embedding_blob": embedding_to_blob(emb)}; flush in batches of _PHASE3_BATCH_SIZE.
          - files_embedded = len(records); embed_elapsed measured.

      Step 6 — Pass 3 DBSCAN clustering:
        - Query MediaFile rows with embedding_blob.isnot(None) AND status == 'ok'; collect (id, path, embedding_blob) tuples.
        - If len(ids) < 2: return summary with clusters=0 noise=len(ids) immediately.
        - embeddings_matrix = np.vstack([blob_to_embedding(b) for b in blobs])
        - Filter zero-norm embeddings (per AI-SPEC §6): compute row norms; drop indices with norm < 0.01; log per skipped; if remaining < 2 early return.
        - if len(ids) > cfg.cluster.sparse_threshold: log info "Using sparse DBSCAN"; labels = cluster_embeddings_sparse(emb, eps=eps, min_samples=cfg.cluster.min_samples); used_sparse = True. else: labels = cluster_embeddings(emb, eps=eps, min_samples=cfg.cluster.min_samples); used_sparse = False.
        - cluster_elapsed measured.

      Step 7 — Write cluster_id assignments:
        - Buffer {"path": path, "cluster_id": int(label) if label != -1 else None}; flush in batches of _PHASE3_BATCH_SIZE.

      Step 8 — Compute summary metrics:
        - n_clusters = len(set(l for l in labels if l != -1))
        - n_noise = int((labels == -1).sum())
        - cluster_sizes = list of sizes per cluster (Counter of labels excluding -1)
        - max_cluster_size = max(cluster_sizes) if cluster_sizes else 0
        - oversized_clusters = sum(1 for s in cluster_sizes if s > 50)
        - elapsed = time.monotonic() - start_time

      Step 9 — Return summary dict with the keys listed in the behavior block.

    Define `_print_cluster_summary(summary: dict, eps: float, cfg) -> None`:
      - Build rich.table.Table titled "Cluster Run Summary"; rows for each metric (files total, files embedded this run, skipped cached, clusters found, noise rate, largest cluster, oversized clusters, quality elapsed, embed elapsed, dbscan elapsed, total elapsed); separator section for Config Used (eps, phash_threshold, quality weights).
      - Console().print(table).
      - Warnings (per AI-SPEC §7): if max_cluster_size > 50, print yellow WARNING line; if noise_rate > 0.70, print yellow warning; if oversized_clusters > 3, print yellow warning.

    Define the click command (use main.command() decorator). Decorate with @click.option for --config (required, click.Path validating exists/readable) and --threshold (default=None, type=float). @click.pass_context to read verbose/quiet. The function body calls _run_cluster(config_path, threshold=threshold, verbose=verbose, quiet=quiet); if not quiet, re-load cfg and call _print_cluster_summary(summary, eps=(threshold if threshold is not None else cfg.cluster.eps), cfg=cfg).

    Do NOT introduce tqdm in this command (RESEARCH.md Q8). Do NOT call upsert_media_file from inside any worker context — workers yield dicts; main-thread for-loop handles upsert+commit.
  </action>
  <verify>
    <automated>cd "C:/Users/jeffr/OneDrive/Documents/Claude Working/PhotoConsole" &amp;&amp; python -c "from click.testing import CliRunner; from photoconsole.cli import main; r=CliRunner().invoke(main, ['cluster','--help']); assert r.exit_code==0, r.output; assert '--config' in r.output and '--threshold' in r.output; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -E '^def _run_cluster' photoconsole/cli.py` returns 1 line
    - Source assertion: `grep -E '^def _print_cluster_summary' photoconsole/cli.py` returns 1 line
    - Source assertion: `grep -E '^def cluster\(' photoconsole/cli.py` returns 1 line (click command function)
    - Source assertion: `grep '@main.command' photoconsole/cli.py` returns >= 5 occurrences
    - Source assertion: `grep 'rich.progress' photoconsole/cli.py` returns >= 1 line
    - Source assertion: `grep 'cluster_embeddings_sparse' photoconsole/cli.py` returns >= 1 line
    - Source assertion: `grep 'embedding_blob.isnot' photoconsole/cli.py` returns >= 2 lines (invalidation query + clustering query)
    - Source assertion: `grep 'os.startfile' photoconsole/cli.py` returns 0 lines (os.startfile belongs to _run_review added in Task 2)
    - CLI assertion: `photoconsole cluster --help` (via CliRunner) prints `--config` and `--threshold`
    - Regression: `pytest tests/test_cli.py tests/test_cli_phase2.py -q` exit 0
  </acceptance_criteria>
  <done>
    cli.py contains _run_cluster, _print_cluster_summary, cluster click command; help text exposes --config and --threshold; existing CLI tests unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement _run_review pure helper + review-clusters click command in cli.py</name>
  <files>photoconsole/cli.py</files>
  <read_first>
    - photoconsole/cli.py (after Task 1 — adds to the same file)
    - photoconsole/clustering.py (Plan 05 — ClusterGroup + write_cluster_manifest)
    - photoconsole/quality.py (Plan 04 — normalize_scores + compute_combined_score + exif_completeness_score)
    - .planning/phases/03-visual-clustering-ai-integration/03-CONTEXT.md (D-12, D-13, D-14, D-15, D-16)
    - .planning/phases/03-visual-clustering-ai-integration/03-PATTERNS.md (`photoconsole/cli.py` os.startfile pattern, batch flush, Rich table)
    - .planning/phases/03-visual-clustering-ai-integration/03-RESEARCH.md (Q3 review session state; Q5 os.startfile behavior; Pitfall 5 path resolve; Pitfall 7 Rich Table + pytest output capture)
  </read_first>
  <behavior>
    - `photoconsole review-clusters --help` exits 0; lists --config and --manifest-dir options
    - review-clusters queries clusters WHERE cluster_id IS NOT NULL AND cluster_keeper IS NULL, sorted by cluster_id
    - For each cluster: prints a Rich table with one row per member (cluster_id, path-truncated, sharpness_norm, resolution_norm, faces_norm, exif_score, combined_score, AUTO-PICK marker)
    - For each cluster: opens top 3 candidates (or fewer if cluster has <3 members) via os.startfile(str(Path(p).resolve())) wrapped in try/except OSError
    - For each cluster: prompts the user (click.prompt) — default "1" = accept auto-pick; "2"/"3"/N = override; "s" = skip
    - On confirmation: upsert_media_file sets cluster_keeper=True on chosen, cluster_redundant=True on non-keepers; commit per cluster
    - At end of session: collect rows where cluster_redundant=True and pass to write_cluster_manifest(rows, manifest_dir). Default manifest_dir is the directory containing catalog_path.
    - --manifest-dir overrides default (created via mkdir parents=True exist_ok=True)
    - On skip: cluster_keeper stays NULL; cluster reappears on next review-clusters run
    - Returns dict summary: clusters_reviewed, clusters_skipped, keepers_confirmed, redundants_marked, manifest_csv_path, manifest_bat_path
  </behavior>
  <action>
    Append to cli.py (after Task 1's cluster command). Do NOT replace anything.

    Define `_run_review(config_path: str, manifest_dir: str | None = None, verbose: bool = False, quiet: bool = False, _prompt_fn=None, _open_fn=None) -> dict`:

      The _prompt_fn and _open_fn keyword parameters default to None and exist for test injection: production uses click.prompt and os.startfile. Fall back inside the function: prompt_fn = _prompt_fn or click.prompt; open_fn = _open_fn or os.startfile. This lets test_cli_phase3.py inject Mock prompts and a no-op open function.

      Step 1 — preflight + thread guard:
        - assert main thread
        - cfg = load_config(config_path)
        - engine = create_catalog_engine(cfg.catalog_path)
        - SessionLocal = session_factory(engine)

      Step 2 — query pending clusters:
        - With session: select(MediaFile).where(MediaFile.cluster_id.isnot(None), MediaFile.cluster_keeper.is_(None), MediaFile.cluster_redundant.is_(None)).order_by(MediaFile.cluster_id) — collect.
        - Group by cluster_id using itertools.groupby on the sorted list. For each group of members:
          - After groupby, skip any ClusterGroup where len(members) < 2: logger.info("Skipping single-member cluster %d", cluster_id) and continue to next group. This is consistent with D-05 (only clusters with 2+ similar photos are reported).
          - For each member compute raw signals dict {sharpness: blur_score or 0.0, resolution: (pixel_width or 0) * (pixel_height or 0), faces: face_count or 0}.
          - Call normalize_scores on a list of result-shaped dicts (one per member) to get normalized values per-cluster (normalization is per-cluster, not global — keeper selection is a within-cluster comparison).
          - For each member: exif_score = exif_completeness_score({"date_taken": m.date_taken, "gps_lat": m.gps_lat, "gps_lon": m.gps_lon}).
          - For each member: combined = compute_combined_score(norm_for_member, exif_score, cfg.quality_weights).
          - Build component_scores dict {m.path: {"sharpness": norm.sharpness_norm, "resolution": norm.resolution_norm, "faces": norm.faces_norm, "exif": exif_score, "combined": combined}}.
          - After computing combined_score for each member, cache it: scores_cache[m.path] = combined (where scores_cache is a dict initialized before the per-member loop in this Step 3).
          - Sort members by combined desc. keeper = sorted[0]. redundants = sorted[1:].
          - Instantiate ClusterGroup(cluster_id=cid, members=sorted_members, keeper=keeper, redundants=redundants, component_scores=component_scores).

      Step 3 — interactive loop:
        - keepers_confirmed = 0; redundants_marked = 0; clusters_reviewed = 0; clusters_skipped = 0.
        - For each ClusterGroup cg:
          - If quiet: skip the visual review and increment clusters_skipped (D-14 safety: non-interactive runs do not silently flag for deletion).
          - Build Rich Table titled f"Cluster {cg.cluster_id} — {len(cg.members)} photos"; columns: Cluster ID, Path, Sharpness, Resolution, Faces, EXIF, Score, AUTO-PICK (green style for the marker).
          - For each member: add row with truncated path (last 40 chars), formatted floats for component scores (3 decimals), combined score to 1 decimal, "[AUTO-PICK]" only on the highest-scored row.
          - Console().print(table).
          - Open top 3 candidates: for p in (cg.keeper, *cg.redundants[:2]): try: open_fn(str(Path(p.path).resolve())) except OSError: log warning via Console "[yellow]Could not open {p.path}: no default viewer registered[/]".
          - choice = prompt_fn(f"Confirm keeper for cluster {cg.cluster_id} [1=auto-pick, 2..{len(cg.members)}=override, s=skip]", default="1", type=str)
          - choice handling:
            - "s" / "S" -> increment clusters_skipped; continue
            - "1" / empty -> chosen = cg.keeper
            - numeric N in [1, len(cg.members)] -> chosen = cg.members[N-1]
            - invalid -> log error, treat as skip and continue
          - With SessionLocal() as session: upsert_media_file(session, {"path": chosen.path, "cluster_keeper": True}); for each other in cg.members where other is not chosen: upsert_media_file(session, {"path": other.path, "cluster_redundant": True}); session.commit().
          - keepers_confirmed += 1; redundants_marked += len(cg.members) - 1; clusters_reviewed += 1.

      Step 4 — manifest write:
        - With session: select(MediaFile).where(MediaFile.cluster_redundant.is_(True)) — collect rows. (Use .is_(True) not == True per SQLAlchemy 2.0 nullable Boolean rules — RESEARCH.md Pitfall 8.)
        - Build manifest row list per redundant MediaFile m: {"original_path": str(Path(m.path).resolve()) if m.source_type == 'local' else m.path, "hash": m.hash, "source_name": m.source_name, "cluster_id": m.cluster_id, "quality_score": scores_cache.get(m.path, 0.0), "source_type": m.source_type}. The scores_cache dict (populated in Step 3) is the authoritative source for quality_score; do not use 0.0 as a fallback for rows that were reviewed (they will be in scores_cache).
        - manifest_path = Path(manifest_dir) if manifest_dir else Path(cfg.catalog_path).parent; manifest_path.mkdir(parents=True, exist_ok=True).
        - csv_p, bat_p = write_cluster_manifest(rows, manifest_path).
        - Log INFO with manifest file paths.

      Step 5 — return dict {clusters_reviewed, clusters_skipped, keepers_confirmed, redundants_marked, manifest_csv_path: str(csv_p), manifest_bat_path: str(bat_p)}.

    Define the click command using @main.command(name="review-clusters") so the CLI-visible name is hyphenated. Decorate with @click.option for --config (required, click.Path validating exists/readable), --manifest-dir (default=None, click.Path(file_okay=False, writable=True)). Use @click.pass_context to receive the group context. Read verbose and quiet from ctx.obj: verbose = ctx.obj.get("verbose", False); quiet = ctx.obj.get("quiet", False). This mirrors the _run_report pattern in cli.py — the --quiet flag is on the main group, not the subcommand, so it must be threaded through ctx.obj. The function body: summary = _run_review(config_path, manifest_dir=manifest_dir, verbose=verbose, quiet=quiet); if not quiet: Console().print(f"Reviewed {summary['clusters_reviewed']} clusters; skipped {summary['clusters_skipped']}; manifest: {summary.get('manifest_csv_path')}").

    The Python identifier for the click command function is `review_clusters` (underscore), but the CLI surface name is `review-clusters` (hyphen) per FR5 convention.

    Do NOT use tqdm. Use Rich only.
  </action>
  <verify>
    <automated>cd "C:/Users/jeffr/OneDrive/Documents/Claude Working/PhotoConsole" &amp;&amp; python -c "from click.testing import CliRunner; from photoconsole.cli import main; r=CliRunner().invoke(main, ['review-clusters','--help']); assert r.exit_code==0, r.output; assert '--config' in r.output and '--manifest-dir' in r.output; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - Source assertion: `grep -E '^def _run_review' photoconsole/cli.py` returns 1 line
    - Source assertion: `grep 'name="review-clusters"' photoconsole/cli.py` returns 1 line
    - Source assertion: `grep -E '^def review_clusters\(' photoconsole/cli.py` returns 1 line
    - Source assertion: `grep 'os.startfile' photoconsole/cli.py` returns >= 1 line (now allowed inside _run_review)
    - Source assertion: `grep 'write_cluster_manifest' photoconsole/cli.py` returns >= 1 line
    - Source assertion: `grep 'cluster_keeper' photoconsole/cli.py` returns >= 2 lines (query filter + upsert)
    - Source assertion: `grep 'cluster_redundant' photoconsole/cli.py` returns >= 2 lines (query filter + upsert + manifest collection)
    - Source assertion: manifest query uses `.is_(True)` not `== True` for cluster_redundant filter: `grep 'cluster_redundant.is_' photoconsole/cli.py` returns >= 1 line
    - Behavior assertion: manifest CSV contains non-zero quality_score values for cluster_redundant rows (scores_cache populated from Step 3 combined_score computation)
    - CLI assertion: `photoconsole review-clusters --help` (via CliRunner) prints `--config` and `--manifest-dir`
    - Regression: `pytest tests/test_cli.py tests/test_cli_phase2.py -q` exit 0
  </acceptance_criteria>
  <done>
    cli.py contains _run_review and review-clusters click command; help text exposes both options; CLI command name is hyphenated; existing CLI tests unchanged.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create tests/test_cli_phase3.py with CliRunner integration tests for cluster + review-clusters</name>
  <files>tests/test_cli_phase3.py</files>
  <read_first>
    - tests/test_cli_phase2.py (in full — for _write_config / _seed_catalog helpers, CliRunner invocation patterns, isolated_filesystem usage)
    - tests/test_cli.py (Phase 1 CliRunner patterns)
    - photoconsole/cli.py (after Tasks 1 and 2)
    - photoconsole/quality.py (Plan 04)
    - photoconsole/clustering.py (Plan 05)
    - .planning/phases/03-visual-clustering-ai-integration/03-PATTERNS.md (`tests/test_cli_phase3.py` section)
    - .planning/phases/03-visual-clustering-ai-integration/03-VALIDATION.md (Per-Task Verification Map rows 03-05-01 + 03-06-01)
  </read_first>
  <behavior>
    - TestClusterCommandPreflight.test_cluster_help_lists_options: --help exits 0, lists --config and --threshold
    - TestClusterCommandPreflight.test_cluster_missing_config_fails: invoke without --config -> click usage error (exit_code != 0)
    - TestClusterCommandPreflight.test_cluster_nonexistent_config_fails: --config /does/not/exist -> exit_code != 0
    - TestClusterCommandPreflight.test_cluster_invalid_yaml_fails: malformed YAML -> non-zero exit with descriptive message
    - TestClusterIncrementalSkip.test_cluster_skips_files_with_embedding_blob_set: seed catalog with 2 rows, one with embedding_blob set; mock embed_images; invoke cluster; assert embed_images called with paths list excluding the cached file
    - TestClusterIncrementalSkip.test_cluster_skips_files_with_blur_score_set: similar for quality pass — files with blur_score NOT NULL skipped
    - TestClusterIncrementalSkip.test_cluster_invalidates_embedding_on_mtime_change: seed row with embedding_blob set + mtime=1000.0; create real file at path with current mtime; invoke cluster (mock embed_images to track); assert path appears in embed_images call (because it was invalidated)
    - TestClusterSparseFallback.test_cluster_uses_sparse_above_threshold: monkeypatch ClusterConfig.sparse_threshold to 3; seed catalog with 5 embedding rows; mock cluster_embeddings_sparse and cluster_embeddings; invoke cluster; assert sparse called and dense NOT called
    - TestClusterSparseFallback.test_cluster_uses_dense_below_threshold: sparse_threshold=100; seed 5 rows; invoke; assert dense called and sparse NOT called
    - TestClusterThresholdOverride.test_threshold_flag_passed_to_dbscan: invoke with --threshold 0.5; mock cluster_embeddings; assert it was called with eps=0.5
    - TestClusterPreflightFails.test_missing_cv2_raises_with_install_hint: monkeypatch sys.modules['cv2']=None and force ImportError; invoke; assert exit_code != 0 and "opencv-python-headless" in output OR exception
    - TestReviewClustersCommandPreflight.test_review_help_lists_options: --help exits 0, lists --config and --manifest-dir
    - TestReviewClustersNoPendingClusters.test_no_pending_clusters_yields_zero_summary: catalog has rows but none with cluster_id IS NOT NULL AND cluster_keeper IS NULL -> exit_code 0, no prompt, clusters_reviewed==0
    - TestReviewClustersKeeperPersistence.test_review_default_keeper_persisted: seed cluster with 2 members; invoke with input="1\n"; after run, query catalog: assert chosen.cluster_keeper == True and other.cluster_redundant == True
    - TestReviewClustersOverride.test_review_user_overrides_auto_pick: seed cluster with 3 members; invoke with input="2\n"; after run, the 2nd member is keeper
    - TestReviewClustersSkip.test_review_skip_leaves_cluster_pending: seed cluster; invoke with input="s\n"; assert no rows have cluster_keeper set; assert manifest is empty (or contains no rows from this cluster)
    - TestReviewClustersManifestWrite.test_manifest_csv_created: after review, csv file exists in manifest_dir with correct header
    - TestReviewClustersManifestWrite.test_manifest_bat_created: .bat file exists with UTF-8 BOM and del /f line for the redundant
    - TestReviewClustersOsStartfileFallback.test_open_fn_oserror_does_not_crash: inject _open_fn that raises OSError; review still completes; warning logged
    - TestReviewClustersQuiet.test_quiet_mode_skips_all_clusters: invoke with --quiet (via ctx.obj override OR --quiet group flag); assert all clusters reported as skipped, no keeper persisted
  </behavior>
  <action>
    Create new test module `tests/test_cli_phase3.py`. Structure conventions from tests/test_cli_phase2.py.

    Imports: pytest, unittest.mock (patch, MagicMock), pathlib.Path, click.testing (CliRunner), photoconsole.cli (main), photoconsole.catalog (create_catalog_engine, session_factory, upsert_many), photoconsole.catalog.models (MediaFile). Use `pytest.importorskip("open_clip")` and `pytest.importorskip("torch")` at module top OR per-test as needed; many tests can mock embed_images so they do not require open_clip importable.

    Module-level helper `_write_config_phase3(config_path, catalog_path, source_path, **kwargs)`:
      - Write a minimal YAML to config_path including catalog_path, sources (one local source pointing to source_path), and optional cluster/quality_weights overrides via kwargs (e.g., kwargs may include {"cluster": {"eps": 0.30, "sparse_threshold": 3}}). Use yaml.safe_dump or textwrap.dedent + f-string. Return str(config_path).

    Module-level helper `_seed_catalog(catalog_path, rows: list[dict])`:
      - engine = create_catalog_engine(str(catalog_path)); SessionLocal = session_factory(engine); with session: for r in rows: upsert_media_file(session, r); commit. Convenience for inserting test rows with specific column values (e.g., embedding_blob, cluster_id, blur_score).

    Test classes:

    `class TestClusterCommandPreflight`:
      - test_cluster_help_lists_options(self) — CliRunner().invoke(main, ["cluster", "--help"]); assert exit_code 0 and "--config" + "--threshold" in result.output
      - test_cluster_missing_config_fails(self) — invoke without --config; assert non-zero exit
      - test_cluster_nonexistent_config_fails(self, tmp_path) — invoke with --config /does/not/exist.yaml; non-zero exit
      - test_cluster_invalid_yaml_fails(self, tmp_path) — write garbage YAML; invoke; non-zero exit; "invalid" or "Error" in output

    `class TestClusterIncrementalSkip`:
      - test_cluster_skips_files_with_embedding_blob_set(self, tmp_path) — _write_config; create real source dir with 2 small JPEG fixtures; _seed_catalog with 2 rows: one with embedding_blob=b"\\x00"*2048, one without; patch photoconsole.cli.embed_images (or photoconsole.clustering.embed_images depending on import path) with MagicMock returning np.zeros((1, 512), dtype=np.float32); invoke cluster; assert the mock was called and the args[0] (paths list) contains only the row that was missing embedding_blob.
      - test_cluster_skips_files_with_blur_score_set(self, tmp_path) — similar pattern; patch compute_quality_scores; assert call args exclude rows with blur_score NOT NULL.
      - test_cluster_invalidates_embedding_on_mtime_change(self, tmp_path) — seed row with embedding_blob=b"old"*683 (avoid empty), mtime=1000.0; create actual file at row's path with current mtime (will differ); patch embed_images; invoke; assert path appears in args[0].

    `class TestClusterSparseFallback`:
      - test_cluster_uses_sparse_above_threshold(self, tmp_path) — write config with cluster.sparse_threshold=3; seed 5 rows with valid embedding_blob (use np.random.randn(512).astype(np.float32) and embedding_to_blob); patch photoconsole.cli.cluster_embeddings_sparse and photoconsole.cli.cluster_embeddings (both with MagicMock returning np.zeros(5, dtype=int)); invoke cluster; assert sparse.called and not dense.called.
      - test_cluster_uses_dense_below_threshold(self, tmp_path) — write config with sparse_threshold=100; seed 5 rows; same mocks; assert dense.called and not sparse.called.

    `class TestClusterThresholdOverride`:
      - test_threshold_flag_passed_to_dbscan(self, tmp_path) — seed 2 rows with embeddings; patch photoconsole.cli.cluster_embeddings with MagicMock; invoke cluster --threshold 0.5; assert the mock's call_args kwargs include eps=0.5 (or positional matching the documented signature).

    `class TestReviewClustersCommandPreflight`:
      - test_review_help_lists_options(self) — CliRunner().invoke(main, ["review-clusters", "--help"]); assert exit_code 0 and "--config" + "--manifest-dir" in output

    `class TestReviewClustersNoPendingClusters`:
      - test_no_pending_clusters(self, tmp_path) — seed catalog with rows where cluster_id IS NULL (or cluster_keeper IS NOT NULL); invoke review-clusters; assert exit_code 0 and "Reviewed 0 clusters" or equivalent in output.

    `class TestReviewClustersKeeperPersistence`:
      - test_review_default_keeper_persisted(self, tmp_path, monkeypatch) — seed 2 catalog rows with cluster_id=1, blur_score values such that row A scores higher; both with cluster_keeper IS NULL and cluster_redundant IS NULL; monkeypatch os.startfile to a no-op (lambda *a, **k: None); invoke review-clusters with input="1\\n"; query catalog after invocation; assert A.cluster_keeper == True and B.cluster_redundant == True.
      - Use either: (a) monkeypatch attribute on cli module's `os` reference, OR (b) inject via the _open_fn parameter — which is not exposed on the click command but is on _run_review. Preferred: monkeypatch os.startfile globally for the test using monkeypatch.setattr("os.startfile", lambda *a, **k: None, raising=False) since os.startfile only exists on Windows.

    `class TestReviewClustersOverride`:
      - test_review_user_overrides_auto_pick(self, tmp_path, monkeypatch) — seed 3 rows in cluster_id=1 with blur scores such that auto-pick = row A; invoke with input="2\\n"; assert row B has cluster_keeper=True and row A has cluster_redundant=True.

    `class TestReviewClustersSkip`:
      - test_review_skip_leaves_cluster_pending(self, tmp_path, monkeypatch) — seed cluster; invoke with input="s\\n"; assert no row in cluster has cluster_keeper IS NOT NULL.

    `class TestReviewClustersManifestWrite`:
      - test_manifest_csv_created(self, tmp_path, monkeypatch) — seed 2-member cluster; invoke with input="1\\n" and --manifest-dir tmp_path/manifests; assert (tmp_path/manifests).glob("cluster_deletion_manifest_*.csv") returns non-empty; read CSV; assert header contains "original_path,hash,source_name,cluster_id,quality_score".
      - test_manifest_bat_created(self, tmp_path, monkeypatch) — similar; assert .bat file exists; read bytes; assert starts with b"\\xef\\xbb\\xbf" and contains "del /f".

    `class TestReviewClustersOsStartfileFallback`:
      - test_open_fn_oserror_does_not_crash(self, tmp_path, monkeypatch) — monkeypatch os.startfile to raise OSError("no viewer"); seed cluster; invoke with input="1\\n"; assert exit_code 0 (review still completes); inspect output for the "no default viewer registered" warning text.

    `class TestReviewClustersQuiet`:
      - test_quiet_mode_skips_all(self, tmp_path, monkeypatch) — seed cluster; invoke with main args `["--quiet", "review-clusters", "--config", config]` (NOTE: --quiet is on the group, not the subcommand); assert no clusters confirmed (all reported skipped in summary).

    All tests use tmp_path for paths. Mock external system calls (os.startfile, embed_images, load_clip_model) so tests run on any OS without dependency on a Windows photo viewer or a multi-GB CLIP model load. Use `monkeypatch.setattr("photoconsole.cli.load_clip_model", lambda: (MagicMock(), MagicMock()))` to bypass CLIP load.

    For the cluster tests, use real ID checking via the catalog after invocation: open create_catalog_engine on the same catalog_path and select rows to validate state changes.
  </action>
  <verify>
    <automated>cd "C:/Users/jeffr/OneDrive/Documents/Claude Working/PhotoConsole" &amp;&amp; pytest tests/test_cli_phase3.py -v --timeout=120</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `tests/test_cli_phase3.py`
    - Source assertion: `grep -E '^class Test(ClusterCommandPreflight|ClusterIncrementalSkip|ClusterSparseFallback|ClusterThresholdOverride|ReviewClustersCommandPreflight|ReviewClustersNoPendingClusters|ReviewClustersKeeperPersistence|ReviewClustersOverride|ReviewClustersSkip|ReviewClustersManifestWrite|ReviewClustersOsStartfileFallback|ReviewClustersQuiet)' tests/test_cli_phase3.py` returns >= 10 lines
    - Source assertion: `grep '^\s*def test_' tests/test_cli_phase3.py` returns >= 18 lines
    - Source assertion: `grep 'CliRunner' tests/test_cli_phase3.py` returns >= 5 lines
    - Source assertion: `grep 'monkeypatch.setattr.*os.startfile\\|monkeypatch.setattr."os.startfile"' tests/test_cli_phase3.py` returns >= 1 line
    - Behavior assertion: `pytest tests/test_cli_phase3.py -v --timeout=120` exit 0 with all tests PASSED (use importorskip for CLIP-dependent paths to skip cleanly on CI without open_clip)
    - Behavior assertion: `pytest tests/test_cli_phase3.py::TestReviewClustersKeeperPersistence -v` exit 0 (proves D-15 persistence)
    - Behavior assertion: `pytest tests/test_cli_phase3.py::TestReviewClustersManifestWrite -v` exit 0 (proves D-16 manifest)
    - Regression: `pytest tests/test_cli.py tests/test_cli_phase2.py -q` exit 0
  </acceptance_criteria>
  <done>
    test_cli_phase3.py exists with 18+ tests covering both commands (cluster preflight, incremental skip, sparse fallback, threshold override; review-clusters keeper persistence, override, skip, manifest writing, OSError fallback, quiet mode); existing CLI tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CLI args (--config, --threshold, --manifest-dir) -> _run_cluster / _run_review | User-controlled CLI flags enter the helpers |
| os.startfile(path) -> Windows shell | Catalog-stored paths handed to ShellExecuteW |
| Catalog rows -> write_cluster_manifest BAT | Original paths from catalog written to .bat file |
| click.prompt -> review session state | User input determines keeper/redundant flags |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-21 | Tampering | --config path traversal | mitigate | click.Path(exists=True, dir_okay=False, readable=True) validates the path before load_config is called (same pattern as Phase 1/2 commands per T-05-01). |
| T-03-22 | Tampering | --threshold injection of non-numeric values | mitigate | click.option(type=float) coerces; invalid values raise click.BadParameter. eps_range_note in ClusterConfig also warns on suspicious values. |
| T-03-23 | Tampering | os.startfile path injection | mitigate | Paths come from catalog rows (already-validated). Always pass `str(Path(p).resolve())` per Pitfall 5 — resolves symlinks and prevents relative-path ambiguity. Wrap in try/except OSError (Q5) to handle the no-default-viewer case gracefully. |
| T-03-24 | Repudiation | Keeper/redundant flags set without audit log | accept | The catalog itself is the audit log: cluster_keeper / cluster_redundant timestamps via scanned_at column. The deletion manifest BAT contains generation timestamp + per-row record. Sufficient for solo-user audit needs per NFR1 (Phase 2 D-15 same pattern). |
| T-03-25 | DoS (user) | Oversized cluster (>50 photos) overwhelming review UI | mitigate | Per AI-SPEC §6 per-cluster gate: log warning, flag cluster as oversized in summary, but do not crash. The user can still review or skip. eps tuning is the remedy (warned in summary). |
| T-03-26 | Information Disclosure | Manifest CSV/BAT containing absolute paths | accept | Paths are already user-controlled (came from catalog). Manifest written to user-provided dest_dir; filesystem permissions are OS responsibility. Same disposition as Phase 2 D-12. |
| T-03-27 | Tampering | click.prompt accepting arbitrary input that bypasses keeper logic | mitigate | Input parsed with explicit choice validation: "s" / "1" / numeric N in range / invalid. Invalid input is treated as "skip" (safe default — no flags written) rather than crashing or auto-confirming. |
| T-03-28 | DoS (memory) | Loading entire catalog into memory for clustering | mitigate | Per AI-SPEC §4b.4: sparse fallback kicks in at sparse_threshold (default 50000). _run_cluster dispatches automatically based on len(files_with_embed). |
</threat_model>

<verification>
- `pytest tests/test_cli_phase3.py -v --timeout=120` exit 0 with all tests PASSED (mocks bypass CLIP load; importorskip guards open_clip-dependent paths)
- `pytest tests/test_cli.py tests/test_cli_phase2.py -q` exit 0 (no regression on Phase 1/2 CLI tests)
- `photoconsole cluster --help` lists --config and --threshold options
- `photoconsole review-clusters --help` lists --config and --manifest-dir options
- `grep -c '^def _run_cluster' photoconsole/cli.py` == 1
- `grep -c '^def _run_review' photoconsole/cli.py` == 1
- `grep -c 'name="review-clusters"' photoconsole/cli.py` == 1
- `grep -c 'os.startfile' photoconsole/cli.py` >= 1
- `grep -c 'cluster_embeddings_sparse' photoconsole/cli.py` >= 1
- `grep -c 'embedding_blob.isnot' photoconsole/cli.py` >= 2
</verification>

<success_criteria>
- cli.py exposes cluster and review-clusters commands as hyphenated CLI surfaces
- _run_cluster pure helper runs all 4 passes (quality, pHash, embedding with invalidation, DBSCAN) with single-writer main-thread commits
- _run_review pure helper queries pending clusters, prints Rich tables, opens top 3 candidates via os.startfile (with OSError fallback), prompts the user, persists keeper/redundant flags, writes the deletion manifest
- Sparse DBSCAN dispatch is automatic based on ClusterConfig.sparse_threshold
- --threshold flag overrides ClusterConfig.eps
- All test_cli_phase3.py tests pass (18+ tests; CLIP-dependent ones gated by importorskip)
- Phase 1/2 CLI tests still pass
</success_criteria>

<output>
Create `.planning/phases/03-visual-clustering-ai-integration/03-06-SUMMARY.md` when done
</output>
