---
phase: 03-visual-clustering-ai-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - tests/conftest.py
autonomous: true
requirements:
  - NFR2
  - FR6
user_setup: []
tags:
  - dependencies
  - test-infrastructure
  - pytest
must_haves:
  truths:
    - "All Phase 3 packages are declared in [project.dependencies]"
    - "pytest markers 'critical' and 'slow' are registered in pyproject.toml"
    - "conftest.py exposes _make_sharp_jpg/_make_blurry_jpg/_make_near_duplicate fixture helpers reusable by Phase 3 tests"
  artifacts:
    - path: "pyproject.toml"
      provides: "Phase 3 deps + pytest markers"
      contains: "open-clip-torch"
    - path: "tests/conftest.py"
      provides: "Synthesized image fixture helpers"
      exports: ["_make_sharp_jpg", "_make_blurry_jpg", "_make_near_duplicate"]
  key_links:
    - from: "pyproject.toml"
      to: "open-clip-torch, imagehash, scikit-learn, opencv-python-headless, pydantic, scipy"
      via: "[project.dependencies]"
      pattern: "open-clip-torch>=3.3"
    - from: "tests/conftest.py"
      to: "numpy + PIL"
      via: "fixture image synthesis"
      pattern: "Image.fromarray"
---

<objective>
Add Phase 3 runtime + dev dependencies and pytest test infrastructure (markers + fixture helpers) required by every downstream Phase 3 plan.

Purpose: Without packages installed and conftest helpers, all Wave 2+ plans will fail at import time. This plan is the dependency bedrock of the entire phase.

Output: Updated `pyproject.toml` with 6 new runtime packages, 1 new dev package, registered pytest markers; extended `tests/conftest.py` with three image-synthesis helpers used by `test_quality.py`, `test_clustering.py`, and `test_eval_clustering.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-visual-clustering-ai-integration/03-CONTEXT.md
@.planning/phases/03-visual-clustering-ai-integration/03-RESEARCH.md
@.planning/phases/03-visual-clustering-ai-integration/03-PATTERNS.md
@.planning/phases/03-visual-clustering-ai-integration/03-AI-SPEC.md
@.planning/phases/03-visual-clustering-ai-integration/03-VALIDATION.md

@pyproject.toml
@tests/conftest.py

<interfaces>
Existing pyproject.toml structure (verified by read):
- [build-system] requires setuptools>=75.0
- [project] requires-python = ">=3.12"
- [project.dependencies] currently has: Pillow, piexif, click, tqdm, PyYAML, SQLAlchemy
- [project.optional-dependencies] dev: pytest>=9.0, pytest-mock>=3.15
- [tool.pytest.ini_options] currently has: testpaths=["tests"], addopts="-ra -q"
  -- NOTE: no `markers = [...]` key currently exists

RESEARCH.md verified package versions (per `## Standard Stack` table):
- open-clip-torch 3.3.0 (NOT 2.24.0 — AI-SPEC has stale version)
- imagehash 4.3.2
- scikit-learn 1.8.0
- opencv-python-headless 4.13.0.92
- pydantic 2.13.4
- scipy 1.17.1
- pytest-benchmark 5.2.3

Per `## Package Legitimacy Audit` table in RESEARCH.md: all 7 packages slopcheck-verified [OK]. Disposition: Approved. No [SUS]/[SLOP] packages.

RESEARCH.md Q7 (verified fixture synthesis) numbers — must replicate exactly:
- Sharp checkerboard (alternating 255/0 pixels) blur_score ≈ 388,183
- Blurry solid-color image blur_score = 0.0
- Near-duplicate via GaussianBlur(radius=1) at JPEG quality=85: Hamming distance ≈ 2
- Different random images: Hamming distance ≈ 36

Existing tests/conftest.py (must be EXTENDED, not replaced) — read in full before editing.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add Phase 3 packages, dev tooling, and pytest markers to pyproject.toml</name>
  <files>pyproject.toml</files>
  <read_first>
    - pyproject.toml (current contents — must extend, not replace)
    - .planning/phases/03-visual-clustering-ai-integration/03-RESEARCH.md (Standard Stack table, Package Legitimacy Audit, pyproject.toml Changes Required section)
  </read_first>
  <behavior>
    - Phase 3 runtime packages installable from PyPI when pip install -e ".[dev]" runs
    - pytest -m critical and pytest -m slow filter to the right tests (no PytestUnknownMarkWarning)
    - pytest-benchmark importable via dev extras
  </behavior>
  <action>
    Extend the existing `[project.dependencies]` array with exactly these six new entries (versions per RESEARCH.md Standard Stack — D-01/D-02/D-04/D-08): `open-clip-torch>=3.3.0,<4`, `imagehash>=4.3.1,<5`, `scikit-learn>=1.4,<2`, `opencv-python-headless>=4.9,<5`, `pydantic>=2.0,<3`, `scipy>=1.11,<2`. Do NOT remove or reorder existing entries (Pillow, piexif, click, tqdm, PyYAML, SQLAlchemy). Do NOT pin torch directly — let open-clip-torch resolve it (per RESEARCH.md Version Notes).

    Extend the existing `[project.optional-dependencies].dev` array with `pytest-benchmark>=4.0,<6` (preserve pytest and pytest-mock entries).

    Add a `markers` key to the existing `[tool.pytest.ini_options]` section (do not remove `testpaths` or `addopts`). Markers list:
      - `critical: critical eval dimensions -- must pass before any merge`
      - `slow: throughput benchmarks -- run manually, not in CI`

    Preserve the `rich` dependency status: rich is documented in RESEARCH.md as "already installed" but is NOT in the current pyproject.toml dependencies. Add `rich>=13,<16` to `[project.dependencies]` so Phase 3 CLI commands have a declared dependency (D-12: Rich tables for review-clusters).

    File ordering: keep `[build-system]` first, `[project]` second, `[project.optional-dependencies]` third, `[project.scripts]` fourth, `[tool.setuptools.packages.find]` fifth, `[tool.pytest.ini_options]` last (current ordering).
  </action>
  <verify>
    <automated>python -c "import tomllib; d=tomllib.loads(open('pyproject.toml','rb').read().decode()); deps=d['project']['dependencies']; assert any('open-clip-torch' in x for x in deps), 'open-clip-torch missing'; assert any('imagehash' in x for x in deps), 'imagehash missing'; assert any('scikit-learn' in x for x in deps), 'scikit-learn missing'; assert any('opencv-python-headless' in x for x in deps), 'opencv missing'; assert any('pydantic' in x for x in deps), 'pydantic missing'; assert any('scipy' in x for x in deps), 'scipy missing'; assert any('rich' in x for x in deps), 'rich missing'; marks=d['tool']['pytest']['ini_options']['markers']; assert any('critical' in m for m in marks), 'critical marker missing'; assert any('slow' in m for m in marks), 'slow marker missing'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `pyproject.toml` parses as valid TOML via `python -c "import tomllib; tomllib.loads(open('pyproject.toml','rb').read().decode())"` exit 0
    - `[project.dependencies]` contains all 7 new strings: `open-clip-torch>=3.3.0,<4`, `imagehash>=4.3.1,<5`, `scikit-learn>=1.4,<2`, `opencv-python-headless>=4.9,<5`, `pydantic>=2.0,<3`, `scipy>=1.11,<2`, `rich>=13,<16`
    - `[project.dependencies]` STILL contains all original entries: Pillow, piexif, click, tqdm, PyYAML, SQLAlchemy
    - `[project.optional-dependencies].dev` contains `pytest-benchmark>=4.0,<6` AND the original pytest/pytest-mock entries
    - `[tool.pytest.ini_options].markers` is a list containing 2 strings starting with `critical:` and `slow:`
    - `[tool.pytest.ini_options].testpaths` is still `["tests"]` and `addopts` is still `"-ra -q"`
    - After `pip install -e ".[dev]"`, `python -c "import open_clip, imagehash, sklearn, cv2, pydantic, scipy, rich"` exits 0 (manual verification — install step is environment-specific)
  </acceptance_criteria>
  <done>
    pyproject.toml updated; tomllib parses it; required package strings and marker entries are present; existing entries preserved.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend tests/conftest.py with Phase 3 image-synthesis fixture helpers</name>
  <files>tests/conftest.py</files>
  <read_first>
    - tests/conftest.py (current contents — must extend, not replace; the existing `in_memory_engine` stub returning None must remain so Phase 1/2 tests keep passing)
    - .planning/phases/03-visual-clustering-ai-integration/03-RESEARCH.md (Q7: Test Fixture Creation Strategy — verified synthesis recipe with measured values)
    - .planning/phases/03-visual-clustering-ai-integration/03-PATTERNS.md (Fixture generation block under `tests/test_clustering.py`)
  </read_first>
  <behavior>
    - _make_sharp_jpg writes a JPEG whose cv2.Laplacian variance exceeds 10,000 (checkerboard high-frequency pattern)
    - _make_blurry_jpg writes a JPEG whose cv2.Laplacian variance is below 50 (solid-color low-frequency)
    - _make_near_duplicate writes a JPEG whose imagehash.phash Hamming distance to its source is <= 10
    - All three helpers accept a Path and an optional size tuple; default size=(200, 200); use JPEG quality=95 for sharp/blurry, quality=85 for near-duplicate (matches RESEARCH.md Q7 recipe)
    - Helpers are plain top-level module functions (not pytest fixtures) so tests can call them directly inside parametrize blocks or other fixtures
  </behavior>
  <action>
    Append (do NOT replace) three top-level helper functions to `tests/conftest.py`: `_make_sharp_jpg(path, size=(200, 200))`, `_make_blurry_jpg(path, size=(200, 200))`, and `_make_near_duplicate(source_path, dest_path)`.

    Implementation pattern per RESEARCH.md Q7 (verified working in research session):
    - sharp: numpy array of shape (height, width, 3) dtype uint8, zeros, then `arr[::2, ::2] = 255` to make a checkerboard; save as JPEG quality=95 via `PIL.Image.fromarray(arr).save(str(path), "JPEG", quality=95)`.
    - blurry: numpy array filled with constant 128, dtype uint8; save as JPEG quality=95.
    - near_duplicate: `PIL.Image.open(source_path).filter(PIL.ImageFilter.GaussianBlur(radius=1))` then save as JPEG quality=85.

    Imports needed (add at top of conftest.py if missing): `import numpy as np`, `from PIL import Image, ImageFilter`. These are already transitive dependencies — Pillow is a project dep; numpy is a transitive dep of scikit-learn/imagehash added in Task 1.

    Do not delete or modify the existing `in_memory_engine` stub or any other existing conftest content. Preserve module docstring.

    Use Path-or-str-compatible signatures: accept `path: Path | str` and call `str(path)` before passing to PIL.
  </action>
  <verify>
    <automated>cd "C:/Users/jeffr/OneDrive/Documents/Claude Working/PhotoConsole" &amp;&amp; python -c "import sys; sys.path.insert(0, 'tests'); from conftest import _make_sharp_jpg, _make_blurry_jpg, _make_near_duplicate; import tempfile, os; td=tempfile.mkdtemp(); sp=os.path.join(td,'s.jpg'); bp=os.path.join(td,'b.jpg'); nd=os.path.join(td,'n.jpg'); _make_sharp_jpg(sp); _make_blurry_jpg(bp); _make_near_duplicate(sp, nd); assert os.path.exists(sp) and os.path.exists(bp) and os.path.exists(nd); import cv2; sharp_var=cv2.Laplacian(cv2.cvtColor(cv2.imread(sp), cv2.COLOR_BGR2GRAY), cv2.CV_64F).var(); blur_var=cv2.Laplacian(cv2.cvtColor(cv2.imread(bp), cv2.COLOR_BGR2GRAY), cv2.CV_64F).var(); print(f'sharp_var={sharp_var:.1f} blur_var={blur_var:.1f}'); assert sharp_var &gt; 10000, f'sharp too low {sharp_var}'; assert blur_var &lt; 50, f'blur too high {blur_var}'; import imagehash; from PIL import Image; h1=imagehash.phash(Image.open(sp)); h2=imagehash.phash(Image.open(nd)); print(f'hamming={h1-h2}'); assert (h1-h2) &lt;= 10, f'near-dup hamming too far {h1-h2}'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/conftest.py` defines top-level callable `_make_sharp_jpg`, `_make_blurry_jpg`, `_make_near_duplicate`
    - Source assertion: `grep -E '^def _make_(sharp|blurry|near)' tests/conftest.py` returns 3 lines
    - Source assertion: `grep -E '^(import numpy|from PIL import)' tests/conftest.py` returns at least 2 lines covering numpy and PIL imports
    - Behavior assertion: Sharp JPEG's grayscale Laplacian variance > 10,000 (validated by `cv2.Laplacian(gray, cv2.CV_64F).var()` after `cv2.imread + cv2.cvtColor(..., COLOR_BGR2GRAY)`)
    - Behavior assertion: Blurry JPEG's grayscale Laplacian variance < 50
    - Behavior assertion: `imagehash.phash(Image.open(sharp)) - imagehash.phash(Image.open(near_dup))` returns int <= 10
    - Existing tests must still pass: `pytest tests/test_catalog.py tests/test_config.py -q` exits 0
  </acceptance_criteria>
  <done>
    tests/conftest.py extended with three verified fixture helpers; numerical thresholds match RESEARCH.md Q7 measured values; no regression on Phase 1/2 tests.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| PyPI -> developer machine | New runtime + dev packages downloaded and installed |
| Test fixture filesystem | tmp_path-scoped JPEGs written by helpers in conftest.py |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | pip install of 7 PyPI packages | mitigate | RESEARCH.md `## Package Legitimacy Audit` confirms slopcheck [OK] on all 7 packages; no [ASSUMED]/[SUS]/[SLOP]. Approved disposition recorded. No blocking-human checkpoint required per planner protocol (all packages [OK]). |
| T-03-01 | Tampering | tests/conftest.py PIL.Image.open in _make_near_duplicate | accept | Source path is always developer-controlled (test fixture written moments earlier in same tmp_path). No user input crosses this boundary. |
| T-03-02 | DoS (self) | pytest marker collision with pre-existing `slow`/`critical` marks in other files | accept | No existing tests use these marks (verified by absence in tests/). Adding markers is purely additive. |
</threat_model>

<verification>
- Run `python -c "import tomllib; tomllib.loads(open('pyproject.toml','rb').read().decode())"` — exit 0
- Run `pip install -e ".[dev]"` then `python -c "import open_clip, imagehash, sklearn, cv2, pydantic, scipy, rich, pytest_benchmark"` — exit 0
- Run `pytest tests/ -m critical --collect-only 2>&1 | grep -v "PytestUnknownMarkWarning"` — no warning lines (mark registered)
- Run `pytest tests/test_catalog.py tests/test_config.py -q` — exit 0 (no regression)
- Verify conftest fixture helpers produce expected blur_score and Hamming distance values (see Task 2 automated verify command)
</verification>

<success_criteria>
- pyproject.toml contains all 7 new runtime packages + pytest-benchmark dev package + critical/slow markers
- tests/conftest.py exports three top-level fixture helpers that produce measured-correct blur_score and Hamming distance values
- Existing Phase 1/2 test suite still passes
- `pip install -e ".[dev]"` succeeds in a clean environment
</success_criteria>

<output>
Create `.planning/phases/03-visual-clustering-ai-integration/03-01-SUMMARY.md` when done
</output>
